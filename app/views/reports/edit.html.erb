<h1><%= t('views.common.title_edit', name: Report.model_name.human) %></h1>

<%= render 'form', report: @content %>

<%= link_to t('views.common.show'), @content %> |
<%= link_to t('views.common.back'), user_reports_path(@content.user_id) %>
