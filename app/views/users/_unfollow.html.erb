<%= form_with(model: current_user.active_follows.find_by(followed_id: @user.id),
            html: { method: :delete }, local: true) do |f| %>
<%= f.submit t('follow.unfollow'), class: "btn btn-default" %>
<% end %>
