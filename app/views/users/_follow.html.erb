<%= form_with(model: current_user.active_follows.build, local: true) do |f| %>
<div><%= hidden_field_tag :followed_id, @user.id %></div>
<%= f.submit t('follow.followed'), class: "btn btn-primary" %>
<% end %>
